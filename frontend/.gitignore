# Dépendances
/node_modules
# Store pnpm local (créé par un install dans le conteneur via le bind mount)
/.pnpm-store

# Build Next.js
/.next
/out
next-env.d.ts

# Divers
.env*.local
*.tsbuildinfo
.DS_Store
